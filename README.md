# Yazi config

My Yazi configuration for WSL.

## Install

Run: git clone REPO_URL ~/.config/yazi

If ~/.config/yazi already exists, back it up first.

## Notes

- i runs searchjump with Chinese pinyin initial matching through sjch.
- Markdown previews use glow through piper.
- The default opener uses Windows Explorer through WSL.
